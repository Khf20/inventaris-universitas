<form method="POST" action="{{ $action }}" class="space-y-5">@csrf @if ($method !== 'POST') @method($method) @endif
    <div><label class="block text-sm font-medium text-slate-700" for="barang_id">Barang</label><select class="mt-1 block w-full rounded-lg border-slate-300" id="barang_id" name="barang_id" required><option value="">Pilih barang</option>@foreach ($barangs as $barang)<option value="{{ $barang->id }}" @selected(old('barang_id', $transaksi?->barang_id) == $barang->id)>{{ $barang->nama }} (stok: {{ $barang->jumlah }})</option>@endforeach</select>@error('barang_id')<p class="mt-1 text-sm text-rose-600">{{ $message }}</p>@enderror</div>
    <div><label class="block text-sm font-medium text-slate-700" for="jumlah">Jumlah</label><input class="mt-1 block w-full rounded-lg border-slate-300" id="jumlah" name="jumlah" type="number" min="1" value="{{ old('jumlah', $transaksi?->jumlah ?? 1) }}" required>@error('jumlah')<p class="mt-1 text-sm text-rose-600">{{ $message }}</p>@enderror</div>
    <div><label class="block text-sm font-medium text-slate-700" for="tgl_pinjam">Tanggal pinjam</label><input class="mt-1 block w-full rounded-lg border-slate-300" id="tgl_pinjam" name="tgl_pinjam" type="date" value="{{ old('tgl_pinjam', $transaksi?->tgl_pinjam?->format('Y-m-d') ?? now()->format('Y-m-d')) }}" required>@error('tgl_pinjam')<p class="mt-1 text-sm text-rose-600">{{ $message }}</p>@enderror</div>
    <div><label class="block text-sm font-medium text-slate-700" for="tgl_kembali">Tanggal kembali</label><input class="mt-1 block w-full rounded-lg border-slate-300" id="tgl_kembali" name="tgl_kembali" type="date" value="{{ old('tgl_kembali', $transaksi?->tgl_kembali?->format('Y-m-d')) }}">@error('tgl_kembali')<p class="mt-1 text-sm text-rose-600">{{ $message }}</p>@enderror</div>
    <div><label class="block text-sm font-medium text-slate-700" for="status">Status</label><select class="mt-1 block w-full rounded-lg border-slate-300" id="status" name="status" required><option value="dipinjam" @selected(old('status', $transaksi?->status ?? 'dipinjam') === 'dipinjam')>Dipinjam</option><option value="dikembalikan" @selected(old('status', $transaksi?->status) === 'dikembalikan')>Dikembalikan</option></select>@error('status')<p class="mt-1 text-sm text-rose-600">{{ $message }}</p>@enderror</div>
    <div class="flex justify-end gap-3"><a href="{{ route('transaksi.index') }}" class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold text-slate-700">Batal</a><button class="rounded-lg bg-slate-900 px-4 py-2 text-sm font-semibold text-white">Simpan</button></div>
</form>
